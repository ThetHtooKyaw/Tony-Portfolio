import 'dart:ui';

class AppColor {
  AppColor._();

  // static const primary = Color(0xFF);
  // static const secondary = Color(0xFF);
  static const accent = Color(0xFF6D110D);
  static const card = Color(0xFF1A1A1A);

  static const white = Color(0xFFFFFFFF);
  static const light = Color(0xFFBDBDBD);
  static const background = Color(0xFF0F0F0F);
  static const shadow = Color(0xFF2B1217);

  static const disable = Color(0xFF3B3B3B);
  static const placeholder = Color(0xFF696969);
}
